spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.150.104/catgal?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&characterEncoding=utf8
    username: root
    password: catgal2024
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000

  data:
    redis:
      host: 192.168.150.104
      port: 6379
      password: catgal2024
      database: 0
      timeout: 5000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0

  rabbitmq:
    host: 192.168.150.104
    port: 5672
    username: catgal
    password: catgal2024
    virtual-host: /

    # ========== AI 配置 ==========
  ai:
    dashscope:
      api-key: ${API_KEY}  # 替换成你的真实 API Key
      chat:
        options:
          model: qwen-plus
          temperature: 0.8
      embedding:
        options:
          model: text-embedding-v4  # 使用最新的 V4 版本
          dimensions: 1024  # 可选，V3/V4 支持自定义维度




